[core]
	autocrlf = false
	fileMode = true
	eol = lf
[merge]
	tool = meld
[mergetool]
	keepBackup = false
[push]
	default = current
[alias]
    untag = "!sh -c 'git tag -d $0 && git push origin :refs/tags/$0'"
